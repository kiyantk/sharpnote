import React from "react";

const MenuBar = () => {
  return (
    <div className="menu-bar">
      <button>File</button>
      <button>Edit</button>
      <button>View</button>
    </div>
  );
};

export default MenuBar;
